# Dockerfile for Actuhub Express Node.js Backend
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
RUN npm run build:server || true
EXPOSE 3000
ENV NODE_ENV=production
CMD ["node", "dist/server.cjs"]
