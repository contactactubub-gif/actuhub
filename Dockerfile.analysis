# Dockerfile for Python Image Forensics Service
FROM python:3.10-slim
WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends \
    libgl1-mesa-glx libglib2.0-0 libgomp1 ffmpeg \
    && rm -rf /var/lib/apt/lists/*
COPY python/image_forensics/requirements.txt ./
RUN pip install --no-cache-dir -r requirements.txt
COPY python/image_forensics/ ./
EXPOSE 5000
CMD ["python3", "main.py"]
